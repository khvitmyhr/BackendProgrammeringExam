testdata: false
spring:
  datasource:
    url: jdbc:h2:mem:mydb
    username: sa
    password:
    driverClassName: org.h2.Driver
    generate-unique-name: false
    name: machine_fabric_test
    test: true
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update